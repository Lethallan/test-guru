  <% if resource.errors.present? %>
    <div>
      <h2>WTF?! <%= pluralize(resource.errors.count, 'error') %>... oh, shi...</h2>

      <ul>
        <% resource.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>